# Verifactu Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# =============================================================================
# AEAT Environment
# =============================================================================

# Environment: sandbox or production
VERIFACTU_ENV=sandbox

# =============================================================================
# Certificate Configuration
# =============================================================================

# Option 1: File path (for local development)
CERT_PATH=./certs/certificate.pfx
CERT_PASSWORD=your-certificate-password

# Option 2: Base64 encoded (for cloud environments)
# CERT_BASE64=base64-encoded-certificate-data
# CERT_PASSWORD=your-certificate-password

# Option 3: PEM files (alternative format)
# CERT_PEM_PATH=./certs/certificate.pem
# CERT_KEY_PATH=./certs/private-key.pem
# CERT_KEY_PASSWORD=your-key-password
# CERT_CA_PATH=./certs/ca-chain.pem

# =============================================================================
# Software Information (required by AEAT)
# =============================================================================

# Your company's NIF (tax ID of software developer)
DEVELOPER_TAX_ID=B12345678

# Software name (as registered with AEAT)
SOFTWARE_NAME=Mi Aplicacion Facturacion

# Software version
SOFTWARE_VERSION=1.0.0

# Installation number (unique per installation)
INSTALLATION_NUMBER=001

# System type: V (Verifactu only) or M (Multiple systems)
SYSTEM_TYPE=V

# =============================================================================
# Optional: Client Configuration
# =============================================================================

# Request timeout in milliseconds (default: 30000)
# TIMEOUT_MS=30000

# Maximum concurrent requests (default: unlimited)
# MAX_CONCURRENCY=5

# Queue timeout in milliseconds (default: 30000)
# QUEUE_TIMEOUT_MS=30000

# =============================================================================
# Optional: Retry Configuration
# =============================================================================

# Maximum retry attempts (default: 3)
# MAX_RETRIES=3

# Initial delay in milliseconds (default: 1000)
# INITIAL_DELAY_MS=1000

# Maximum delay in milliseconds (default: 30000)
# MAX_DELAY_MS=30000
